# 重複音樂人工撤下紀錄

更新：2026-09-18 01:31 Asia/Taipei。

## 授權與範圍

- 使用者試聽後指出兩筆投稿為同一首音樂，明確回覆「保留加註版本，撤下原歌名」。這是 owner 人工內容處置，不是系統自動重複音訊判定。
- 撤下 `e5608206-eab6-41ad-875d-37446be531b5`，歌名「夢を描き始める場所」。
- 保留 `95450736-b981-442a-9da5-6eb9bf49097e`，歌名「夢を描き始める場所 (グランドフィナーレ)」。
- 操作為撤下公開展示，不刪 row 或 Storage 音檔，不刪歷史互動、合併票數、退回上傳額度或處罰帳號。

## 證據與原因

- 兩筆為同一投稿者，建立時間為 2026-09-17 04:52:35 UTC 與 04:54:46 UTC。
- 資料庫兩筆 `audio_sha256` 不同；儲存的時長分別為 1040 與 391 秒。本次沒有自行執行音訊內容比對，不能由 metadata 宣稱兩檔完全相同或推論投稿者惡意。
- 目前 `src/app/listen-bar/page.tsx` 的上傳防重檢查比對活躍歌曲 `audio_sha256`，只能命中相同雜湊的檔案，不能替代音訊指紋。

## 正式操作

- Supabase project `rwueinzgjaaefjvmsyem`，單筆條件包含撤下 ID、精確歌名、投稿者 ID、active 與 removed_at 為空，避免誤動其他資料。
- 第一筆 update 的說明不符合現有人工移除 trigger 慣例，被拒絕，未寫入。
- 查明 trigger 後，沿用既有允許的 `Moderation:` 說明標記，記載 owner 明確授權、保留版本 ID 及保留音檔／互動。未停用或修改 trigger。
- 成功時間為 2026-09-17 17:28:14.820497 UTC，即台灣 2026-09-18 01:28:14。僅更新 is_active=false、review_status=removed、hidden_at、removed_at、moderation_note。
- 撤下回傳 heart_count=4；此前讀取為 3，公開保留曲的支持數也於操作期間更新。操作未寫入任何 Heart／reaction／榜單分數欄位，不能把自然互動變動誤報成票數轉移。

## 驗證與後續

- 正式月榜 API 回 200，26 首，撤下 ID 消失、保留 ID 仍在。當時保留曲 rank=1、supporterCount=4；即時數值非凍結承諾。
- 酒吧 API 回 200，195 首，撤下 ID 消失、保留 ID 仍在。
- 探索第一次讀取為舊快取；帶核對參數重新讀取回 200、cache MISS、195 首，撤下 ID 消失、保留 ID 仍在。已開啟頁面需重新整理取得新列表；撤下不代表銷毀原公開音檔 URL。
- 本次無應用改碼、DB migration 或 Vercel 部署。只提交處置紀錄與私人 roadmap。
- owner 同票裁定、後台待辦提示與加強防重為新需求，尚未上線。設計方向統一記在私人 roadmap；現行月榜運算未於本次改寫。
