# AIPOGER Drop Battle v3 — Codex Task List

> Generated by Mavis on 2026-06-03.  
> 8 tasks total. **Task 1 already done** (commit `ac711d4`). Tasks 2-8 here.  
> 可以一次餵全部給 Codex，或分批餵（建議分批，有相依順序）。

---

## 上下文 (Context)

- **專案**: `aipoger-web`（Next.js 16 + Supabase + Vercel + Tailwind 4）
- **分支**: `codex/aipoger-ui-redesign`
- **既有最新 commits**:
  - `ac711d4` fix(battle): 關閉 legacy waiting room 90s 倒數
  - `91ebf37` style(i18n): 語言切換器
  - `8deee8c` refactor(nav): 統一導航策略
  - `4c64b76` feat(battle): 統一 90s battle 勝負判定與 no contest 規則
- **重點檔案**:
  - `src/lib/battle-90s-system.ts` — 戰鬥邏輯常數與函式（含 `pick90sBattleWinner`、`PRE_BATTLE_TEASER_SECONDS`、`WAITING_ROOM_SECONDS=0`）
  - `src/app/battle/[id]/battle-room-client.tsx` — 戰場主頁（2961 行）
  - `src/app/battle/setup/page.tsx` — setup 表單
  - `src/lib/battle-pool-client.ts` — queue 邏輯
  - `supabase/migrations/` — 既有 migration（按時間前綴，找最近的 `battle_queue*.sql` 跟 `battles*.sql` 看 schema）
  - `vercel.json` — cron 設定
- **業務規則**（記在 `agent.md`）: 0 觀眾票 = no contest（已實作）

---

## 依賴順序 (Order)

```
Task 2 (DB migration) ← 最先
    ↓
🆕 Task 6.5 (battles schema 修正) ← 補進來，Task 6 前必做
Task 3 (setup UI) + Task 4 (arena 倒數) + Task 6 (cron)  ← 平行可做
    ↓
🆕 Task 4.5 (queue → battles 搬 scheduled_start_at) ← 補進來
Task 5 (砍 waiting-room) ← 依賴 Task 3
Task 7 (founder 手動取消) ← 依賴 Task 3, 6, 6.5
    ↓
Task 8 (測試) ← 最後
```

---

## Task 1: ✅ Done
- `WAITING_ROOM_SECONDS` 90 → 0
- Commit `ac711d4`
- 對應 test「legacy waiting room countdown is disabled for direct arena flow」現在通過

## Task 2: ✅ Done
- 新增 `scheduled_start_at` / `cancellation_evaluation_at` / `cancellation_reason` 欄位
- 包含 CHECK CONSTRAINT 跟欄位 COMMENT
- Commit `f3b121c`

## Task 3: ✅ Done
- Setup 頁加 10/15/20 + 自訂時間 UI
- i18n 雙語、graceful fallback、加 unit test
- Commit `e2be191`

## Task 4: ✅ Done
- 戰場倒數讀 `scheduled_start_at`（fallback 到 `started_at`）
- Commit `444b88a`

## Task 4.5: ✅ Done
- 4 條 battle 建立路徑全部搬運（API route、hook-cut、matchmaking、SQL RPC）
- 新增 SQL trigger `copy_queue_schedule_to_battle` 雙保險
- 加 helper `buildDropBattleSchedulePayloadFromQueues` 處理 3 種配對情境
- Commit `0322eae`

## Task 5: ✅ Done
- 砍掉 setup/matchmaking/invite 對 `/battle/waiting-room/` 的 redirect
- waiting-room 路由保留 + deprecation banner「本頁已棄用，請直接進戰場」
- CTA 文案「進入等待場」→「查看戰帖」
- daily battle 流程不動
- Commit `33831ea`

## Task 6: ✅ Done（但需要先跑 Task 6.5 migration）
- 新 API route `src/app/api/battle-pool/cancel-stale-challenges/route.ts`
- vercel.json 加 cron（每分鐘 `* * * * *`）
- CRON_SECRET 環境變數保護
- 完整的 race condition 防護（UPDATE 帶 `.eq("status", "pending").is("fighter_b_user_id", null)`）
- i18n metadata 存進通知
- Commit `ebc04ce`
- ⚠️ **Critical**：production 跑之前必須先跑 `supabase/20260603_battles_pending_and_nullable_fighter_b.sql`（見 Task 6.5）

## 🆕 Task 6.5: battles schema 修正（補進來的）

### 為什麼需要這個 Task
Codex 跑完 Task 6 後指出：battles 表的 schema 不支援 v3 流程。Codex 守住了 Task 6 邊界沒動 schema，結果是 cron 在 production 跑會 100% 失敗：
- `status` enum 沒有 `'pending'` 跟 `'cancelled_no_challenger'`，任何對這兩個值的寫入會被 SQL reject
- `fighter_b_user_id` 是 `NOT NULL`，沒辦法表達「founder 開局但還沒人接」

這個 schema 依賴原本漏在 spec 裡（spec 假設 schema 已經能表達 pending 跟取消，實際上不行）。

### 目標
擴充 `battles.status` enum + 把 `fighter_b_user_id` 改 nullable，支撐 v3 流程。

### 影響檔案
- 新檔案 `supabase/20260603_battles_pending_and_nullable_fighter_b.sql`（已寫好，見檔案）

### 實作內容
1. `ALTER TABLE battles ALTER COLUMN fighter_b_user_id DROP NOT NULL` — 允許 pending 狀態時 NULL
2. `ALTER TABLE battles DROP CONSTRAINT battles_status_check; ADD CONSTRAINT ... CHECK (status IN ('pending', 'live', 'finished', 'cancelled', 'cancelled_no_challenger', 'cancelled_founder'))`
3. 加註解說明每個狀態的用途

### 不做
- 不改 `battles_distinct_fighters` check constraint（NULL <> X 還是 NULL，不會失敗）
- 不動其他表
- 不改 RLS

### 驗證
- 跑 migration 後
- 看 `battles.status` 接受 6 個值
- 看 `battles.fighter_b_user_id` 可以是 NULL
- 跑 Task 6 cron 一次，看是否真的能取消 stale battle

### ⚠️ 跑這個 migration 的時機
**必須在 Task 6 cron 第一次跑之前**。如果 production cron 已經在跑，會在每次 cron 觸發時撞 SQL error 並把錯誤寫進 Vercel logs。

---

## Task 2: DB migration

### 目標
新增 `scheduled_start_at`、`cancellation_evaluation_at`、`cancellation_reason` 欄位，支援 v3 流程。

### 影響檔案
- 新檔案 `supabase/20260603_drop_battle_scheduled_start.sql`（用今天日期前綴）

### 實作
1. 讀 `supabase/migrations/` 找最近 `battle_queue*.sql` 跟 `battles*.sql` 看 schema，確認欄位命名風格
2. ALTER TABLE 加欄位：
   - `battle_queue.scheduled_start_at TIMESTAMPTZ NULL`
   - `battle_queue.cancellation_evaluation_at TIMESTAMPTZ NULL`
   - `battles.scheduled_start_at TIMESTAMPTZ NULL`（從 queue 帶過來）
   - `battles.cancellation_evaluation_at TIMESTAMPTZ NULL`
   - `battles.cancellation_reason TEXT NULL`（值域：`'no_challenger'` / `'founder_manual'`）
3. CREATE INDEX：
   - `idx_battles_pending_scheduled` ON battles(scheduled_start_at) WHERE status = 'pending'
4. 在檔頭註解說明每個欄位用途

### 不做
- 不刪除任何現有欄位
- 不改 RLS policy
- 不動既有資料

### 驗證
- 拿到 Supabase SQL editor 跑前先 syntax check（給 user 自己跑，不要在這 commit 裡直接執行）
- Commit message 標明「需手動跑 migration」

---

## Task 3: setup 頁加開戰時間 UI

### 目標
founder 在 setup 頁能選「10 / 15 / 20 分鐘後」或「自訂 24H 內 datetime」。

### 影響檔案
- `src/app/battle/setup/page.tsx`（主要）
- `src/lib/battle-pool-client.ts`（儲存邏輯，確保 `scheduled_start_at` 跟 `cancellation_evaluation_at` 一起送進 queue）

### Context
- 既有 setup 流程：選模式 → 上傳 → 標 AI 工具 → 提交
- 提交後進 `battle_queue` 表
- 用 Tailwind 4，元件風格看既有 setup 表單

### 實作
1. 在 setup form 加「開戰時間」區塊，**位置**：AI 工具欄位後、送出按鈕前
2. UI 規格：
   - 4 顆按鈕（單選）：
     - 「10 分鐘後」/ "10 min later"
     - 「15 分鐘後」/ "15 min later"
     - 「20 分鐘後」/ "20 min later"
     - 「自訂時間」/ "Custom time" — 展開 `<input type="datetime-local">`
   - 預設選「10 分鐘後」
   - 自訂 picker 限制：`min = now + 1min`、`max = now + 24h`
3. 驗證（前端 form validation）：
   - 選的時間不能是過去
   - 自訂時間必須在 24H 內
   - 錯誤時顯示行內紅字提示
4. 送出時把 `scheduled_start_at`（ISO 字串）跟 `cancellation_evaluation_at`（= scheduled_start_at + 60s）一起送進 queue insert

### i18n
- 所有 UI 文字要中英雙語
- 用 `useI18n()` 的 `t()` 或 `lang === "zh"` 判斷

### 不做
- 不動 challenge fee 邏輯
- 不改 setup 流程的其他步驟
- 不重整 form layout

### 驗證
- `npm run build` 通過
- `npx tsc --noEmit` 通過
- 視覺檢查（手動）：
  - 4 顆按鈕排版 OK
  - 自訂 picker 限制生效
  - 預設選 10 分鐘

---

## Task 4: 戰場倒數讀 scheduled_start_at

### 目標
戰場進場後顯示「距離開戰還有 X」倒數，從 `battles.scheduled_start_at` 算。

### 影響檔案
- `src/app/battle/[id]/battle-room-client.tsx`（主要）

### Context
- 既有 `preStartSecondsLeft` state 跟 `isPreBattle` 邏輯（line 768、816）
- 戰場中央已有 pre-battle 區塊（line 2570-2606，含 PLAY 5S 按鈕、preStartClock 顯示）
- `preStartSecondsLeft` 目前是 number | null——Task 4 要把它綁到 `battle.scheduled_start_at`

### 實作
1. 戰場初始化時讀 `battle.scheduled_start_at`（battle data 從 `loadBattle()` 拿）
2. 計算 `preStartSecondsLeft = Math.max(0, Math.floor((new Date(scheduled_start_at).getTime() - Date.now()) / 1000))`
3. 用既有 `setInterval` 每秒更新（如果已經有就調整；如果沒有就新增）
4. 倒數歸 0 時觸發開戰——既有 phase 邏輯應該會處理，確認即可
5. pre-battle 區塊的 `preStartClock` 顯示（line 2576）已經有，確認排版

### 不做
- 不改 PLAY 5S 邏輯
- 不改 RPS 邏輯
- 不改 vinyl disc 樣式

### 驗證
- 設 1 分鐘後開戰 → 戰場倒數從 `0:59` 開始遞減
- 倒數歸 0 後正常進 RPS / battle phase
- 設已過時間（防呆）→ 倒數直接顯示 `0:00` 進入 battle phase

---

## Task 4.5: 🆕 queue → battles 搬運 scheduled_start_at（補進來的依賴）

### 為什麼需要這個 Task
Codex 在跑完 Task 4 後指出來：Task 3 把 `scheduled_start_at` 寫進 `battle_queue`，Task 4 從 `battles.scheduled_start_at` 讀，但**中間沒有人把 queue 的時間搬進 battles 表**。當 queue 配對成功、battle 成立時，battles.scheduled_start_at 還是 null，arena 倒數會 fallback 到 `started_at`（可能壞掉）。

這個依賴原本漏在 spec 裡，Codex 抓得很準。**Task 4.5 必須在 Task 6 自動取消 cron 之前做完**，否則 cron 查詢 `battles.scheduled_start_at` 會全部是 null。

### 目標
當 battle 從 queue 成立時，把 `scheduled_start_at` 跟 `cancellation_evaluation_at` 從 `battle_queue` 帶進 `battles` 表。

### 影響檔案
- `src/app/api/battle-pool/attempt-matchmaking/route.ts`（配對成功時 insert/update battles）
- `src/app/api/battle-pool/process-fallbacks/route.ts`（fallback 配對也會 create battle）
- `supabase/` 看是否有 trigger 或 RPC 也要更新

### Context
- 從之前 grep 知道的 route 位置
- battle_queue 有 `scheduled_start_at` 跟 `cancellation_evaluation_at` 欄位（Task 2 加的）
- battles 表也有相同欄位（Task 2 加的）
- 既有配對成功時應該有 INSERT INTO battles ... 的邏輯

### 實作
1. 找 battle_pool 配對成功的 INSERT/UPSERT battles 邏輯（可能在 attempt-matchmaking 或 process-fallbacks 或其他 route）
2. 在 INSERT/UPDATE 時，把對應 queue row 的 `scheduled_start_at` 跟 `cancellation_evaluation_at` 一起寫進 battles
3. 如果用 SQL trigger / function 配對，也要更新那邊
4. 如果是 client 端 insert，要確認 queue row 跟 battle row 的對應邏輯

### 不做
- 不改配對邏輯本身（只搬欄位）
- 不刪除或修改 queue 表的欄位
- 不改 RLS

### 驗證
- 跑 Task 2 的 migration 後
- 設 10 分鐘後開戰
- 等配對成功 → 進戰場看倒數是否正確（不是 fallback 到 0）
- 進 Supabase 看 battles.scheduled_start_at 是否有值
- 確認 Task 6 cron 跑下去查詢條件 `cancellation_evaluation_at < NOW()` 會 match 到（不是全部 null）

---

## Task 5: 砍 waiting-room 中繼路由

### 目標
setup / matchmaking / invite 完後直接跳戰場，不再進 waiting room。

### 影響檔案
- `src/app/battle/setup/page.tsx`（redirect 邏輯，line 916 附近）
- `src/app/battle/matchmaking/page.tsx`（成功後 redirect）
- `src/app/battle/invite/[id]/page.tsx`（接受後 redirect，line 80/163）
- `src/app/battle/waiting-room/[id]/page.tsx`（加 deprecation banner）
- `src/app/battle/daily/waiting-room/[id]/page.tsx`（同上，**但 daily battle 流程不動**）
- `src/components/global-lang-toggle.tsx`（移除 `/battle/waiting-room` 跟 `/battle/daily/waiting-room` 的隱藏規則）
- `src/components/global-site-presence-badge.tsx`（移除 line 7 的 `HIDDEN_PREFIXES`）

### Context
- 從之前 grep 知道的 redirect 位置
- daily battle 流程（24H）不在這次 v3 範圍，**不要動** `/battle/daily/waiting-room/` 的業務邏輯，只加 deprecation banner

### 實作
1. setup 完：直接 `router.push(/battle/${battleId})` 而非 `/battle/daily/waiting-room/...`
2. matchmaking 成功：直接 `router.push(/battle/${matchId})` 而非 `/battle/waiting-room/...`
3. invite 接受：直接 `router.push(/battle/${id})` 而非 `/battle/waiting-room/...`
4. waiting-room 路由保留（避免硬刪），頁面頂加 banner：「本頁已棄用，請直接進戰場」
5. global-lang-toggle 不再隱藏這些路由
6. presence-badge 不再隱藏

### 不做
- 不刪除檔案（避免 git history 混亂）
- 不動 daily battle 流程（24H 獨立）
- 不改其他 redirect 邏輯

### 驗證
- 完整走一次 setup → 直接進 arena，不經 waiting room
- 老 waiting-room URL（`/battle/waiting-room/xxx`）能進去看得到 deprecation banner
- 確認 daily battle 流程沒壞

---

## Task 6: 自動取消 cron

### 目標
開戰時間 + 1 分鐘時，自動取消「沒人接受挑戰」的 battle，並通知 founder。

### 影響檔案
- 新檔案 `src/app/api/battle-pool/cancel-stale-challenges/route.ts`
- `vercel.json`（加 cron entry）
- `src/lib/battle-pool-client.ts`（加通知邏輯 helper，optional）

### Context
- 既有 `src/app/api/battle-pool/process-fallbacks/route.ts` 是 5min cron 的範本，**照它的風格寫**
- 既有 `battle_notifications` 表（從 supabase migrations 看 schema）—— service role 可以寫入
- vercel.json 既有 cron：
  ```json
  "crons": [
    { "path": "/api/listen-bar/process-rotation", "schedule": "0 */4 * * *" },
    { "path": "/api/battle-pool/process-fallbacks", "schedule": "*/5 * * * *" }
  ]
  ```

### 實作
1. 新 API route `src/app/api/battle-pool/cancel-stale-challenges/route.ts`：
   - 用 service role Supabase client（跟 process-fallbacks 一樣）
   - 查詢條件：
     ```ts
     const now = new Date().toISOString();
     const { data: staleBattles } = await admin
       .from("battles")
       .select("id, fighter_a_user_id, scheduled_start_at, cancellation_evaluation_at")
       .eq("status", "pending")
       .is("fighter_b_user_id", null)
       .lte("cancellation_evaluation_at", now);
     ```
   - 對每個 stale battle：
     - UPDATE `battles` 設 `status = 'cancelled_no_challenger'`、`cancellation_reason = 'no_challenger'`
     - INSERT `battle_notifications` 給 founder：
       - `type: 'battle_cancelled_no_challenger'`
       - `title: '挑戰自動取消'` / `'Battle auto-cancelled'`
       - `body: '你發起的挑戰在開戰時間過後 1 分鐘仍無對手接受，已自動取消。'`
   - 回傳 `{ cancelled: number, errors: string[] }`
2. vercel.json 加 cron：
   ```json
   { "path": "/api/battle-pool/cancel-stale-challenges", "schedule": "* * * * *" }
   ```
3. 確認 `battle_notifications` 表的 RLS 允許 service role 寫入（既有應該就有了）

### 不做
- 不動 24H battle 邏輯
- 不刪除 `process-fallbacks` cron
- 不做 retry / dead letter
- 不做 30s 級 cron（Vercel 最小單位是 1 分鐘，1 分鐘夠用）

### 驗證
- 本地手動觸發 API 看 response
- 設 1 分鐘後開戰，等到時間過 1 分鐘，確認 status 變 `cancelled_no_challenger`
- 確認 founder 收到通知（看 `battle_notifications` 表）

---

## Task 7: founder 手動取消按鈕

### 目標
founder 在戰場頁面（自己發起的）可以「主動取消」挑戰。

### 影響檔案
- `src/app/battle/[id]/battle-room-client.tsx`

### Context
- 戰場中央 pre-battle 區塊（line 2570-2606）有「分享戰帖」按鈕
- 「取消挑戰」按鈕放在分享按鈕附近
- 限制：battle 還沒成立（`fighter_b_user_id` 是 null）時才能取消

### 實作
1. 在 pre-battle 區塊加「取消挑戰」按鈕：
   - 文案：「取消挑戰」/ "Cancel challenge"
   - 顏色：紅色 outline，跟主視覺橘色區隔
2. 按下顯示確認對話框（用 `window.confirm` 簡單做）：
   - 「確定要取消這場挑戰？取消後無法恢復。」
3. 確認後呼叫：
   ```ts
   const { error } = await supabase.from("battles").update({
     status: "cancelled_founder",
     cancellation_reason: "founder_manual"
   }).eq("id", battleId);
   ```
4. UI 更新：按鈕變「已取消」狀態（disabled + 灰底），跳出戰場回 battle 列表（可選，`router.push("/battle")`）
5. battle 已成立（`fighter_b_user_id` 有值）時：取消按鈕 disabled + tooltip「已有人接受挑戰，無法取消」

### 不做
- battle 成立後不能取消
- 不做「撤銷取消」
- 不做 modal 動畫（簡單 confirm 即可）

### 驗證
- founder 在自己戰場能按「取消挑戰」
- 確認後 status 變 `cancelled_founder`、`cancellation_reason = 'founder_manual'`
- 有人接受挑戰後，取消按鈕變 disabled
- 取消後再進戰場看到「已取消」狀態

---

## Task 8: 測試

### 目標
為 v3 新功能加 unit test。

### 影響檔案
- `tests/battle-pool-rules.test.mjs`（擴充）

### Context
- 既有 test 用 `node --test --experimental-strip-types`
- 既有 pattern：import 函式、assert equal
- 既有 14 個 test 都要繼續通過

### 實作
1. 加 test case（看 v3 邏輯實際抽到哪個檔案，可能是 `battle-90s-system.ts` 或新檔）：
   - `scheduled_start_at` validation：
     - 過去時間 → reject
     - 超過 24H → reject
     - 10/15/20 分鐘後 → accept
   - 自動取消判斷：
     - `scheduled_start_at + 60s` 過了 + `fighter_b` 還是 null → 應該取消
     - 已經有 fighter_b → 不取消
   - founder 手動取消：
     - battle 還沒成立時允許
     - battle 成立後不允許
2. 跟著實作改——如果 v3 邏輯抽出 helper，就測 helper

### 不做
- 不做 E2E（playwright，留給 user 之後手動跑）
- 不做整合測試（需要真實 Supabase）
- 不 mock Supabase（v1 純函式測試就好）

### 驗證
- `npm test` 全綠（既有 14 個 + 新增）
- `npm run lint` 沒新增 warning
- `npx tsc --noEmit` 0 errors

---

## 提交規範 (Commit Convention)

每完成一個 task 一次 commit。訊息格式：

| Task | Commit Message |
|---|---|
| 2 | `feat(db): 新增 battle scheduled_start_at 與 cancellation 欄位` |
| 3 | `feat(setup): founder 設定開戰時間 UI（10/15/20 分鐘 + 自訂 24H）` |
| 4 | `feat(arena): 戰場倒數讀 scheduled_start_at` |
| 4.5 | `fix(battle): 配對成功時把 scheduled_start_at 從 queue 帶進 battles` |
| 5 | `refactor(nav): 砍掉 waiting-room 中繼路由，setup/matchmaking/invite 直接跳戰場` |
| 6.5 | `feat(db): battles status enum 擴充 + fighter_b_user_id 改 nullable` |
| 6 | `feat(cron): 自動取消開戰後 1 分鐘仍無對手的 battle（每分鐘 cron）` |
| 7 | `feat(arena): founder 手動取消挑戰按鈕` |
| 8 | `test: v3 scheduled_start_at 與取消規則` |

每個 commit 前跑驗證：
- `npm test` ✅
- `npm run lint` ✅（沒新增 warning）
- `npx tsc --noEmit` ✅
- `npm run build` ✅（如果是改了 routing / 新 API route）

**不要 push**——user 自己 push。

---

## 給 Codex CLI 的 prompt 範本

### 一次餵全部
```
你是一個資深 Next.js / Supabase / TypeScript 工程師，正在幫 AIPOGER (AI 音樂鬥歌平台) 實作 v3 流程。

[貼整份 docs/aipoger-drop-battle-v3-tasks.md]

請從 Task 2 開始，依序完成 Task 2-8。每個 task 一次 commit，commit 訊息用上面表格的格式。完成後回報每個 commit 的 hash 與遇到的問題。
```

### 分批餵（推薦 ⭐，任務相依順序可控）
```
[貼整份文件]
請只做 Task 2（DB migration），做完後給我 commit hash 與 migration 檔路徑，我確認後再做下一個。
```

---

## 不要做的事（Cross-cutting Rules）

1. **不要 push**——user 自己決定 push 時機
2. **不要動 24H Full Song 流程**（daily battle）——v3 範圍只有 Drop Battle
3. **不要刪除檔案**——deprecate 但保留，避免 git history 與 daily battle 參照炸掉
4. **不要改既有 RLS**——除非 Task 6 必要（service role 通常已經有權限）
5. **不要省略 i18n**——所有 UI 文字中英雙語
6. **不要省略 .nvmrc / Node 版本檢查**——看現有設定
7. **記得跑 build**——Task 3/4/5/6/7 都會動到路由或新增檔案

---

最後更新：2026-06-03
